# Copyright 2025 The OpenXLA Authors. All Rights Reserved.
# Apache-2.0 License. See LICENSE for details.
#
# BUG BOUNTY DEMO - OSS VRP
# Mirrors the vulnerable pattern in the real openxla/xla ci.yml:
#   - Triggers on pull_request with no fork gate
#   - Checks out PR head (fork-controlled code)
#   - Executes build_tools/ci/build.py from the fork tree
name: CI
permissions:
  contents: read
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  Tests:
    name: "XLA Linux x86 CPU [BUG BOUNTY DEMO]"
    # In production this is: linux-x86-n2-16 (Google GCE self-hosted runner)
    # Using ubuntu-latest here since the fork lacks those runner labels
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: "Checking out openxla/xla"
        # On a pull_request event this resolves to refs/pull/N/merge,
        # which is the attacker fork's HEAD merged into base - attacker controls build.py
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          path: "openxla/xla"
          persist-credentials: false

      - name: "Run build.py"
        # Executes the fork's build_tools/ci/build.py - NOT the upstream copy.
        # Any fork PR author fully controls this script.
        run: |
          "$GITHUB_WORKSPACE"/openxla/xla/build_tools/ci/build.py \
            --build="XLA Linux x86 CPU [BUG BOUNTY DEMO]_github_actions"
